/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
	--main: #FF5C5B;
}

.center {
	display: flex;
	text-align: center;
	justify-content: center;
}

h1, h2, h3 {
	color: var(--main);
}